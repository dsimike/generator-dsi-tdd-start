language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-dsi-tdd-start' ]; then cd .. && eval "mv $currentfolder generator-dsi-tdd-start" && cd generator-dsi-tdd-start; fi

